import SubscriptionManager from '@/components/SubscriptionManager';

const UserSettings = () => {
  return (
    <div>
      <h1>User Settings</h1>
      <SubscriptionManager />
    </div>
  );
};

export default UserSettings;
